services:
  - type: web
    name: market-opportunity-engine
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --threads 4 --timeout 240
    healthCheckPath: /health
